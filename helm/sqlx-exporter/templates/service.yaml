apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqlx-exporter.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sqlx-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "sqlx-exporter.selectorLabels" . | nindent 4 }}